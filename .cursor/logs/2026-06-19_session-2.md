# Lean Session — 2026-06-19 (Session 2)

**Goal**: Explain and formalize Chapter 2 material in `FormalProofs/2_Lattices.lean` — especially `corollary_2_29_i` (join in a subcollection) and Lemma 2.30 (sup = inf of upper bounds).
**Source**: Blyth-style lattice theory (Ch. 2); Lean file as written.
**Outcome**: Partial — key proofs closed (`corollary_2_29_i`, `isLUB_iUnion_range`, `lemma_2_30`, `lemma_2_30_equality`); conceptual vocabulary established; `lemma_2_28_*` and much of Ch. 2 still open.

---

## Proof Log

### [CONVO] CONVO — /log activated

User invoked `/log`. Session logging started for this conversation.

---

### [CONVO] CONVO — Informal statement of `corollary_2_29_i`

User asked for a plain-language translation of the Lean lemma at lines 231–233, using only what appears in the file (no chat/notes/comments). Explained: indexed family of subsets, membership in `L`, union as least upper bound in `(𝒫(X), ⊆)`.

---

### [CONVO] CONVO — Textbook-style Corollary 2.29(i)

User requested the same lemma rewritten as a corollary in standard textbook form.

---

### [CONVO] CONVO — Restating `IsLUB` inside `L`

User asked how to write the corollary so the join is taken in the sub-poset `L` rather than in ambient `(Set X, ⊆)`. Advised: package family and witness as elements of `L` via `⟨·, h·⟩`, use `Set.range (fun i => ⟨A i, hA i⟩)`, conclude `IsLUB ... ⟨⋃ i, A i, hAL⟩`; prove via ambient `isLUB_iSup` + `lemma_2_28_join`.

---

### [CONVO] CONVO — What is an ambient sup?

User asked for definition of “ambient sup”. Explained: supremum computed in the surrounding poset `P` (e.g. `(Set X, ⊆)`) vs join in a sub-poset `Q ⊆ P`; linked to `IsLUB (Subtype.val '' S) p` vs `IsLUB S ⟨p, hpQ⟩`.

---

### corollary_2_29_i — fix type mismatch in proof

**Attempts**: 2
**Failed** (1):
- `obtain` from `isLUB_iUnion_range` + `exact ⟨x, And.intro hx rfl⟩` — goal is `IsLUB ...`, not `∃`; `And.intro` expected a `Prop`, got `Set X`

**What worked**: Ambient join via `isLUB_iSup`, rewrite range through `Subtype.val`, transfer with `IsLUB.of_image`.
**Why it worked**: Wrong goal shape (existential vs `IsLUB`); sub-poset join needs the packaged element of `L`, not bare `Set X`.

**Final state**: Proved ✓

---

### [CONVO] CONVO — Textbook ⋁ S = ⋀ S^u in Lean

User asked how to express lemma 2.30's conclusion (sup of S equals inf of upper bounds) in Lean. Explained `IsLUB`/`IsGLB (upperBounds S)` pairing, `isGLB_upperBounds` iff, and `sInf (upperBounds S) = sSup S` on complete lattices.

---

### lemma_2_30_result — fix isGLB_upperBounds misuse

**Attempts**: 2
**Failed** (1):
- `isGLB_upperBounds (s := upperBounds S) y hy` — treated iff as function; wrong `s` gave `upperBounds (upperBounds S)`

**What worked**: `(isGLB_upperBounds (s := S)).2 hLUB` for direct equality form; later simplified to `lemma_2_30_equality` without redundant `hInf`.
**Why it worked**: `isGLB_upperBounds` is `↔` with parameter the original set `S`, not `upperBounds S`.

**Final state**: Proved ✓

---

### [CONVO] CONVO — `lemma_2_30` vs `lemma_2_30_equality`

User asked whether the existence lemma and the equality lemma differ. Clarified: `lemma_2_30` proves `∃ x, IsLUB S x` from inf-existence + bounded above; `lemma_2_30_equality` states ⋁ S = ⋀ S^u as `IsGLB (upperBounds S) y ∧ x = y` given `IsLUB S x`.

---

## Tactic & Tool Patterns

| Pattern | Used | Worked | Notes |
|---------|------|--------|-------|
| `isLUB_iSup` + `Set.iSup_eq_iUnion` | 3 | 3 | Ambient join/inf on `(Set X, ⊆)` |
| `IsLUB.of_image` / `IsGLB.of_image` | 2 | 2 | Transfer bounds between sub-poset and ambient via `Subtype.val` |
| `isGLB_upperBounds` (`.1` / `.2`) | 4 | 4 | Core link ⋁ S ↔ ⋀ S^u; parameter must be `S`, not `upperBounds S` |
| `IsLUB.unique` | 1 | 1 | Identifies sup witness with inf-of-upper-bounds witness |
| `lean_multi_attempt` | 2 | 2 | Validated `corollary_2_29_i` proof before edit |
| `lean_leansearch` / `lean_leanfinder` | 3 | 3 | Found `isGLB_upperBounds`, `IsLUB.of_image`, `sInf_upperBounds_eq_csSup` |
| Existential packaging (`⟨x, ...⟩`) | 2 | 1 | Failed when goal was bare `IsLUB`, not `∃` |

**Takeaways**: Distinguish ambient `(Set X, ⊆)` from sub-poset `L` via `Subtype.val` and packaged elements `⟨s, hs⟩`. For Lemma 2.30, use `isGLB_upperBounds (s := S)` — never instantiate `s` as `upperBounds S`. Separate existence (`lemma_2_30`) from equality (`lemma_2_30_equality`).

---

## Session Summary

Session focused on reading and fixing Chapter 2 lattice lemmas in `2_Lattices.lean`. Established vocabulary for ambient vs sub-poset suprema (`IsLUB (Subtype.val '' S)` vs `IsLUB S ⟨p, hpQ⟩`), restated Corollary 2.29(i) with join taken inside `L`, and proved it via `isLUB_iSup` + `IsLUB.of_image`. Fixed a common error pattern: treating `IsLUB` goals as existentials. For Lemma 2.30, clarified the textbook identity ⋁ S = ⋀ S^u as `IsGLB (upperBounds S) x ↔ IsLUB S x` and split existence (`lemma_2_30`) from the equality corollary (`lemma_2_30_equality`). Key insight: `isGLB_upperBounds` is an iff about the **original set** `S`; mis-instantiating it produces `upperBounds (upperBounds S)`.

---

## Next Steps

1. Prove `lemma_2_28_join` / `lemma_2_28_meet` (needed to chain ambient facts into sub-poset joins cleanly).
2. Add meet dual of `corollary_2_29_i` (`IsGLB` / intersection in `L`).
3. Continue `theorem_2_31` TFAE chain (several directions still `sorry`).
